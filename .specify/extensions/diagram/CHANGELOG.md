# Changelog

## 1.0.0 (2026-04-08)

- Initial release
- Add `/speckit.diagram.workflow` command for SDD lifecycle visualization
- Add `/speckit.diagram.status` command for feature progress dashboard
- Add `/speckit.diagram.dependencies` command for task dependency DAG
- Mermaid output renders natively on GitHub, GitLab, and VS Code
- Optional `after_tasks` hook for auto-generating dependency diagrams
- Addresses community request in issue #467 (50+ upvotes)
